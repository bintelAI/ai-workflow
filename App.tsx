import React from 'react';
import { WorkflowApp } from './components/Workflow/WorkflowApp';

/**
 * 应用根组件
 * 用于渲染Workflow应用
 */
const App: React.FC = () => {
  return <WorkflowApp />;
};

export default App;
